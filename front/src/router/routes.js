/** Views */
import main from '@/views/Main.vue';

const routes = [
    { 
        path: '/', component: main,
        meta: {
            title: 'SwApi Home'
        }
    },
];

export default routes;